[mypy]
strict = True

# TensorClass is described by tensorclass.pyi. Avoid analyzing the unrelated,
# dynamically generated TensorDictBase implementation when checking this
# focused consumer regression.
[mypy-tensordict.base]
follow_imports = skip
